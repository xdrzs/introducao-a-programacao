#include <stdio.h>

// Implemente a função a seguir, que recebe dois `int`s, `a` e `b` e retorna o maior entre eles.
int max(int a, int b) {
	return 0;
}

int main() {
	printf("max(0,0) = %d\n", max(0,0));
	printf("max(1,0) = %d\n", max(1,0));
	printf("max(-2,7) = %d\n", max(-2,7));
	printf("max(-300,-4) = %d\n", max(-300,-4));
	printf("max(15,578) = %d\n", max(15,578));
}
